import React, {useState} from "react";
import {useHistory} from "react-router-dom";


//will need user and token
const Cart = () => {
  // we should already have the user
 
  return (
    <h2> Cart </h2>
  )
}

export default Cart